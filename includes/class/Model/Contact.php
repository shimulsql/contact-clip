<?php

    namespace App\Model;

    class Contact extends Model{

        public function create($data){
            // $user_id = $data['user_id'];
            // $group_id = $data['user_id'];
            // $name = $data['name'];
            // $mobile = $data['mobile'];
            // $email = $data['email'];
            // $image = $data['image'];

            return $data;
        }
    }